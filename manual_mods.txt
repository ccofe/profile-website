// Manual modifications
chmod -x npm_installs.sh

// package.json
use the following scripts object
"scripts": {
    "dev": "webpack --mode development --watch",
    "build": "webpack --mode production"
  },
  
//
